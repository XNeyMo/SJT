import { Subject, Course } from '@src/types';
import { generateCombinations, validation, calculatePoints } from '@src/utils';

export function findOneDayLess(subjects: Subject[]): Course[] {
	return [];
}
